### 数组排序之后相邻数的最大差值

#### 题目
给定一个整型数组 array, 返回排序后的相邻两数的最大差值

#### 举例
array = [9, 3, 1, 10], 如果排序, 结果为 [1, 3, 9, 10], 9 和 3 的差为最大差值, 故返回 6  
array = [5, 5, 5, 5], 返回 0

#### 要求
如果 array 的长度为 N, 请做到时间复杂度为 O(N)
